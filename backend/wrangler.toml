name = "ai-stock-backend"
main = "src/main.py"
compatibility_date = "2024-04-03"
compatibility_flags = ["python_workers"]

[vars]
KIS_APP_MODE = "vps"

# Secrets like KIS_API_KEY and KIS_API_SECRET should be set via 
# 'npx wrangler secret put' or in the Cloudflare Dashboard.
